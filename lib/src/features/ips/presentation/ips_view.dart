import 'package:flutter/widgets.dart';

class IpsView extends StatelessWidget {
  const IpsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
